[metadata]
name = demo_package
version = 0.0.0

[options]
packages = find:
install_requires =
    rclpy
    std_msgs
zip_safe = false

[options.entry_points]
console_scripts =
    talker = demo_package.talker:main
    listener = demo_package.listener:main
